import { StyleSheet} from "react-native"
import { colorValues } from "./colors"

export default StyleSheet.create({
    title: {
        fontSize: 48,
        color: colorValues.elements.headline,
        fontWeight: 'bold'
    }
})